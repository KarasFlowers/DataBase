# ===================================================================
# = DATASOURCE
# ===================================================================
# Database connection settings.
# IMPORTANT: Replace with your actual database URL, username, and password.
# spring.datasource.url=jdbc:mysql://localhost:3306/campus_delivery?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.url=jdbc:mysql://124.70.86.207:3306/h_db23373226?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.username=your_username
spring.datasource.username=u23373226
spring.datasource.password=Aa503142
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================================================================
# = JPA / HIBERNATE
# ===================================================================
# Since we are not using Spring Data JPA, we don't need these properties.
# However, if you were to use it, you might configure something like this:
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
